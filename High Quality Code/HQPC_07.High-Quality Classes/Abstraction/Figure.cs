namespace Abstraction
{
    using System;

    abstract class Figure
    {//I think in this case, the abstract class cannot be used, because its children are not that similar, and its use will lead to use of too much repeating code

        //public virtual double Width { get; set; }
        //public virtual double Height { get; set; }
        //public virtual double Radius { get; set; }

        //public Figure()
        //{
        //}

        //public Figure(double radius)
        //{
        //    this.Radius = radius;
        //}

        //public Figure(double width, double height)
        //{
        //    this.Width = width;
        //    this.Height = height;
        //}
    }
}
